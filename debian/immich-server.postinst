#!/bin/sh
set -e

if [ "$1" = "configure" ]; then
  adduser --system --group --quiet --no-create-home --home /var/lib/immich immich >/dev/null || true
  chown immich:immich /var/lib/immich /var/lib/immich/data
fi

#DEBHELPER#
